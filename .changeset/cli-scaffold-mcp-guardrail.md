---
"@antadmin/cli": minor
---

Scaffold thêm cấu hình AI tooling: `.mcp.json` (GitLab self-host + Figma) và
`.claude/settings.json` guardrail (deny đọc `.env`/`~/.npmrc`, deny force-push,
allow sẵn các lệnh lint/typecheck/build/git read-only).
