# Không đưa vào build context (build sạch, không lộ secret, cache nhỏ)
node_modules
.nuxt
.output
.data
.git
.idea
.DS_Store
*.log
*.tsbuildinfo

# Secret local — tuyệt đối không vào image
.env
.env.*
!.env.example

# Không liên quan tới build
Dockerfile
docker-compose*.yml
.gitlab-ci.yml
scripts
README.md
