// Prompt library dùng chung — import từ '@antadmin/ai/prompts'.
export { definePrompt, listPlaceholders, renderPrompt } from './define'
export type { PromptDef } from './define'
export { extractJson, promptLibrary, summarize, antadminAssistant } from './library'
